server:
  port: 81

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/finder?allowPublicKeyRetrieval=true&useSSL=false&useUnicode=true&characterEncoding=utf8&serverTimezone=Europe/Moscow
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: none
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB

logging:
  level:
    root: warn
    org.springframework.web: warn
    xyz.raichu.diplom: info
